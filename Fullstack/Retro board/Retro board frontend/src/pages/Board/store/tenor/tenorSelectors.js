// eslint-disable-next-line import/prefer-default-export
export const getGifLinks = state => state.tenor.gifLinks;

export const getIsModalOpen = state => state.tenor.isModalOpen;

export const getTenorColumnId = state => state.tenor.columnId;

export const getTenorItemId = state => state.tenor.itemId;
